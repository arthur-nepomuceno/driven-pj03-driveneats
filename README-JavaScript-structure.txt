1. MENU FUNCTIONS

1.1
function selectMeal(mealOption) {}
    will select the meal wich recieves the click.
    if clicked again, it deselects.

1.2
function selectDrink(drinkOption) {}
    will select the drink wich recieves the click.
    if clicked again, it deselects.

1.3
function selectDessert(dessertOption) {}
    will select the dessert wich recieves the click.
    if clicked again, it deselects.

===================================================================================
===================================================================================

2. REQUEST BUTTON FUNCTIONS
